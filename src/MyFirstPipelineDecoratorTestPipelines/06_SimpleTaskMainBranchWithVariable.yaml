name: 06_SimpleTaskMainBranchWithVariable

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

variables:
  executeDecoration: 'true'

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'